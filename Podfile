platform :ios, '10.0'

install! 'cocoapods', disable_input_output_paths: true

target 'AnyImageKit' do
  use_frameworks!

  pod 'SnapKit', '~> 5.0'
  pod 'Kingfisher', '~> 6.0'
  
end

target 'Example' do
  use_frameworks!
  
  pod 'AnyImageKit', :path => './'

end
